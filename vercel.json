{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && prisma generate --schema=packages/database/prisma/schema.prisma && pnpm --filter web build",
  "outputDirectory": ".next"
}
