{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "prisma generate --schema=packages/database/prisma/schema.prisma && pnpm --filter web build",
  "outputDirectory": "apps/web/.next"
}
